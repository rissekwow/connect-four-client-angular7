import { ElementRef } from "@angular/core";

export class C4CellCanvas {
    x: number;
    y: number;
    canvasWidth: number;
    canvasHeight: number;
    color: string;
    image: ElementRef;
    fake: boolean;

}